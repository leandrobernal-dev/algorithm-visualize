import { extendTheme } from "@mui/joy/styles";
const theme = extendTheme({
    colorSchemes: {
        light: {
            palette: {},
        },
        dark: {
            palette: {},
        },
    },
});

export default theme;
